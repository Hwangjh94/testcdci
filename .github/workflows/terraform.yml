name: "Terraform Secure CI/CD"

on:
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'modules/**'
      - 'environments/**'
      - '.github/workflows/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  security:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: tfsec scan
        uses: aquasecurity/tfsec-action@v1.0.0
        with:
          working_directory: .

      - name: checkov scan (NIST 등급화 포함)
        uses: bridgecrewio/checkov-action@master
        with:
          directory: .
          quiet: true
          output_format: cli
        continue-on-error: false  # 보안 이슈 발견 시 워크플로우 실패 → PR 차단

  terraform:
    name: Terraform Plan + PR 댓글
    needs: security
    runs-on: ubuntu-latest

    strategy:
      matrix:
        environment: [dev, prod]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.3.0

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions-role
          aws-region: ap-northeast-2

      - name: Terraform Init
        run: terraform init -backend-config=environments/${{ matrix.environment }}/backend.tf
        working-directory: .

      - name: Terraform Validate
        run: terraform validate
        working-directory: .

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan -var-file=environments/${{ matrix.environment }}/terraform.tfvars -no-color \
            -out=tfplan_${{ matrix.environment }}.binary > plan_output.txt || echo "Plan failed" > plan_output.txt
        working-directory: .

      - name: PR 코멘트에 Plan 결과 출력
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('plan_output.txt', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `📌 **Terraform Plan 결과 (${process.env.environment})**\n\`\`\`\n${output}\n\`\`\``
            });
